# Runtime lifecycle

The supplied emulator exposes ARM9 GDB and bounded captures work at entry. Diplomacy overlay execution has not been runtime-captured because available SDL dummy/offscreen backends fail OpenGL/GLES window creation before automated navigation. This limitation does not change the static presentation conclusion.
