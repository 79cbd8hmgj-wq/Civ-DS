# Replacement boundary

`sub_02206610` is **not** a safe authoritative replacement boundary: it presents derived state. A future UI-only hook may wrap it after its input globals and drawing ABI are confirmed. Gameplay replacement must occur at a still-unidentified ARM9 mutation/validation boundary and preserve save/load consumers.
