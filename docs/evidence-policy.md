# Evidence policy

Address domains (runtime, binary-relative, and ROM-relative) are recorded separately. Conclusions are labelled `candidate`, `probable`, `confirmed`, or `rejected`; a semantic claim requires static executable evidence or a controlled runtime observation before it can be confirmed. Raw dumps and copyrighted data stay under ignored `local/`.
