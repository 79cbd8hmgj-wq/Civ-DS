# Civ-DS

Evidence-first inspection and functional-decompilation tooling for the USA multilingual Nintendo DS release of *Sid Meier's Civilization Revolution*.

Copyrighted ROMs and emulator binaries are local inputs and are never project source. See `docs/evidence-policy.md`. Install with `python -m pip install -e .`, then use `python -m civds inspect-rom PATH`.
